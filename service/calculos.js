export default function Calcular({valor:valor}){

    function isInteger(str) {
        var n = Math.floor(Number(str));
        return String(n) === str && n >= 0;
      }


        result = eval(valor);
        return result;


}